<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Flora"
             x:Class="Flora.PickFromThreePage">
    <ContentPage.Content>
        <StackLayout>
            <Label  x:Name="PickFromThreePrompt"
                    Text="How would you like to begin?"
                    VerticalOptions="CenterAndExpand" 
                    HorizontalOptions="CenterAndExpand" />
            <Button x:Name="GpsButton"
                    Margin="0,25,0,0"
                    Text="GPS"
                    Clicked="GpsButton_Clicked"/>
            <Button x:Name="KeyButton"
                    Margin="0,25,0,0"
                    Text="Key"
                    Clicked="KeyButton_Clicked"/>
            <Button x:Name="DirectLookupButton"
                    Margin="0,25,0,0"
                    Text="Direct Lookup"
                    Clicked="DirectLookupButton_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>